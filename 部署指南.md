# 部署和使用完整指南（从零开始，含 Excel 导出）

> 面向完全不会写代码的用户。每一步说明在哪里操作、输入什么命令、看到什么结果。
> 系统：Windows

---

## 全流程概览

```
第1步  安装 Python（一次性）
第2步  打开命令行，进入项目文件夹
第3步  安装程序依赖（一次性）
第4步  全量抓取所有 SKU 认证信息（约60分钟，可跳过）
第5步  提取认证 PDF 中的认证号（约30分钟，可跳过）
第6步  启动 Web 查询服务
第7步  浏览器查询 + 下载 Excel
第8步  让内网同事访问（开放防火墙）
第9步  部署到云端 Render（可选，24小时在线）
第10步 飞书机器人集成（可选）
```

---

## 第1步：安装 Python（只需一次）

### 1.1 下载安装

1. 打开浏览器，访问 https://www.python.org/downloads/
2. 点击黄色的 **"Download Python 3.12.x"** 按钮
3. 下载完成后，双击运行 `python-3.12.x-amd64.exe`
4. **最重要**：安装界面最底部有个复选框
   **"Add Python to PATH"** —— 必须勾选！
   （不勾选的话后面所有命令都会报错）
5. 点 **"Install Now"**，等进度条走完
6. 看到 "Setup was successful" → 点 "Close"

### 1.2 验证

1. 按键盘 `Win + R`（同时按 Windows 键和 R 键）
2. 弹出"运行"窗口，输入 `cmd`，按回车
3. 弹出黑色命令行窗口
4. 输入以下命令，按回车：

   ```
   python --version
   ```

5. 显示 `Python 3.12.x` → 安装成功
   显示"不是内部或外部命令" → 回 1.1 重新安装，勾选 PATH

---

## 第2步：打开命令行，进入项目文件夹

### 2.1 打开命令行

1. 按 `Win + R`，输入 `cmd`，按回车

### 2.2 进入项目文件夹

在命令行里复制粘贴这行命令（复制后在黑色窗口里右键粘贴），按回车：

```
cd /d C:\Users\seeed\Documents\Codex\2026-08-12\h\outputs\seeed-cert-agent
```

### 2.3 确认进对了

输入 `dir` 按回车，应该看到这些文件名：

```
app.py  export.py  scraper.py  requirements.txt  启动服务.bat  ...
```

> ★ 以后每次用命令行操作，都要先做第2步进入项目文件夹

---

## 第3步：安装程序依赖（只需一次）

程序需要 4 个额外组件（flask、requests、pypdf、openpyxl）。

在命令行输入以下命令，按回车：

```
pip install -r requirements.txt
```

等待安装，看到类似输出：

```
Collecting flask...
Collecting requests...
Collecting pypdf...
Collecting openpyxl...
Successfully installed flask requests pypdf openpyxl ...
```

看到 "Successfully installed" → 安装成功

> 如果报错"pip 不是内部或外部命令"：
> 输入 `python -m pip install -r requirements.txt` 再试

---

## 第4步：全量抓取所有 SKU 认证信息

把 seeedstudio.com 上所有产品（约3100个）的认证信息下载到本地数据库。
之后查询就非常快（直接查本地，不联网）。

### 4.1 运行

在命令行输入以下命令，按回车：

```
python scraper.py
```

### 4.2 看到的输出

```
============================================================
Seeed Studio 认证信息全量抓取
============================================================
正在获取 sitemap.xml ...
  共找到 3144 个产品页面
  [1/3144] SKU:102110207 | Raspberry Pi 3 Model A+ | 认证:3个
  [2/3144] SKU:102991022 | Wio LTE EU Version v1.3 | 认证:1个
  ...
  --- 进度: 200/3144 | 成功:195 跳过:0 失败:5 ---
  ...
```

### 4.3 完成后

```
抓取完成！成功:3100 跳过:0 失败:44 总计:3144
```

- 耗时约 60-80 分钟，可以放着不管
- 完成后项目文件夹里多出 `cert_data.db` 数据库文件
- 中途想停 → 直接关窗口；下次运行自动跳过已抓的

> ★ 跳过此步也行，但首次查每个 SKU 会慢5-10秒（在线探测）
> ★ 想更新数据 → 再运行一次 `python scraper.py`（自动跳过24小时内已更新的）

---

## 第5步：提取认证 PDF 中的认证号（新增）

从每个认证 PDF 中提取认证编号（如 FCC 的 Reference No、RoHS 的 Report No）。
这一步会下载每个证书 PDF 并读取内容，提取认证号存入数据库。

### 5.1 运行

在命令行输入以下命令，按回车：

```
python export.py --extract-pdfs
```

### 5.2 看到的输出

```
============================================================
  批量提取认证 PDF 中的认证号
============================================================
共 800 个产品有认证信息，开始提取认证号...
（每个 PDF 间隔 0.5 秒）

  [1/800] SKU:102110207 | Raspberry Pi 3 Model A+ | EUDOC=图片型PDF; ROHS=WTD22X12262119C; UKDOC=图片型PDF
  [2/800] SKU:102991022 | Wio LTE EU Version v1.3 | ROHS=WTD22X12262106C
  ...
```

### 5.3 完成后

```
提取完成！更新:780 跳过:0 失败:20 总计:800
```

- 耗时约 20-30 分钟（取决于有多少产品有认证）
- EUDOC 和 UKDOC 的 PDF 是图片格式，会标注"图片型PDF"
- 其他类型（FCC、CE、RoHS）会提取出认证号

> ★ 如果只想提取个别 SKU 的认证号，可以先运行全量抓取，再运行此步
> ★ 强制重新提取：`python export.py --extract-pdfs --force`

---

## 第6步：启动 Web 查询服务

### 6.1 启动

在命令行输入以下命令，按回车：

```
python app.py --host 0.0.0.0 --port 5000
```

或者直接双击项目文件夹里的 `启动服务.bat`，选菜单第1项。

### 6.2 看到的输出

```
=======================================================
  Seeed Studio 认证查询服务已启动
  本机访问:   http://localhost:5000
  内网访问:   http://192.168.x.x:5000
  数据库:     ...cert_data.db
=======================================================
 * Running on http://0.0.0.0:5000
```

### 6.3 重要

- 这个窗口不要关！关了服务就停了，可以最小化
- 如果端口被占用 → 换端口：`python app.py --port 8080`

---

## 第7步：浏览器查询 + 下载 Excel

### 7.1 打开查询页面

1. 打开浏览器（Chrome / Edge / Firefox）
2. 地址栏输入以下地址，按回车：

   ```
   http://localhost:5000
   ```

   （如果第6步用了别的端口如 8080，就输 http://localhost:8080）

3. 看到查询页面：
   - 标题 "Seeed Studio 产品认证查询"
   - 输入框 + "查询"按钮
   - 绿色的 "📥 下载 Excel 认证映射表" 按钮

### 7.2 查询 SKU

1. 在输入框输入 SKU 号，如：`114993117`
2. 点"查询"按钮（或按回车键）
3. 下方显示结果：

```
SKU: 114993117
产品: EdgeBox RPi 200
认证: RoHS, CE, FCC, TELEC, UKCA

认证文件（PDF）：
  [证书] CE 认证 · 认证号: WTD21D08078434R1E      [查看 PDF]
  [证书] FCC 认证 · 认证号: WTD21D08078438R1E      [查看 PDF]
  [证书] RoHS 认证 · 认证号: WTD22X12262162C       [查看 PDF]
  [证书] UKCA 认证 · 认证号: 图片型PDF             [查看 PDF]

📄 下载产品规格书 PDF
查看 Seeed Studio 产品页面 →
```

4. 点 [查看 PDF] → 浏览器打开认证证书 PDF
5. 点"📄 下载产品规格书" → 打开产品规格 PDF

### 7.3 下载 Excel

1. 点页面上的绿色按钮 **"📥 下载 Excel 认证映射表"**
2. 浏览器自动下载一个 Excel 文件，如：
   `Seeed_Cert_Map_20260812_153000.xlsx`
3. 打开 Excel，内容如下：

| SKU | 产品名称 | 认证类型 | 认证号(PDF内) | 认证PDF链接 | 产品页面 | 更新时间 |
|-----|---------|---------|-------------|-----------|---------|---------|
| 102110207 | Raspberry Pi 3 Model A+ | RoHS 认证 | WTD22X12262119C | https://files... | https://www... | 2026-08-12... |
| 102110207 | Raspberry Pi 3 Model A+ | EUDOC | 图片型PDF | https://files... | https://www... | 2026-08-12... |
| 102991022 | Wio LTE EU | NA | NA | NA | https://www... | 2026-08-12... |

> ★ 如果认证号列显示"未提取"，说明还没运行第5步
> ★ 每个SKU的每种认证占一行；无认证的产品显示一行 NA

### 7.4 也可以用命令行导出 Excel

```
python export.py --excel
```

Excel 生成在项目文件夹里，文件名如 `认证信息映射表_20260812_153000.xlsx`

---

## 第8步：让内网同事访问

### 8.1 开放防火墙端口（只需一次）

1. 按 `Win` 键，搜索 `cmd`
2. **右键** "命令提示符" → 选 **"以管理员身份运行"**
3. 复制粘贴这行命令，按回车：

   ```
   netsh advfirewall firewall add rule name="Seeed Cert Agent" dir=in action=allow protocol=TCP localport=5000
   ```

4. 看到 `确定。` → 成功，关掉管理员窗口

### 8.2 启动服务（绑定了 0.0.0.0，内网可访问）

```
python app.py --host 0.0.0.0 --port 5000
```

### 8.3 把地址发给同事

看终端显示的"内网访问"那行，如：

```
http://192.168.15.146:5000
```

同事在浏览器打开这个地址 → 看到查询页面 → 输入 SKU 查询 / 下载 Excel

> ★ 电脑不能关机/休眠，否则同事无法访问
> ★ IP 可能变，每次启动看终端显示的最新 IP
> ★ 换端口 → 防火墙命令里的 5000 也改成对应端口

---

## 第9步：部署到云端 Render（可选，24小时在线）

### 9.1 上传代码到 GitHub

1. 注册 GitHub：https://github.com/signup
2. 登录后点右上角 **"+"** → **"New repository"**
3. 名称填 `seeed-cert-agent`，选 Public，点 **"Create repository"**
4. 点 **"Add file"** → **"Upload files"**
5. 把项目文件夹里所有文件拖上去（除了 cert_data.db）
6. 点 **"Commit changes"**

### 9.2 注册 Render 并部署

1. 打开 https://render.com，注册（推荐用 GitHub 登录）
2. 点 **"New +"** → **"Web Service"**
3. 连接你的 `seeed-cert-agent` 仓库
4. 配置：
   - Name: `seeed-cert-agent`
   - Runtime: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python app.py --port $PORT`
   - Instance Type: Free
5. 点 **"Create Web Service"**
6. 等 2-3 分钟部署完成
7. 得到公网地址：`https://seeed-cert-agent.onrender.com`

### 9.3 在云端运行抓取和提取

部署后数据库是空的，在 Render 的 Shell 里运行：

```
python scraper.py
python export.py --extract-pdfs
```

> ★ Render 免费版 15分钟无访问自动休眠，下次访问等30秒自动唤醒
> ★ 重新部署会清空数据库，建议本地跑全量抓取

---

## 第10步：飞书机器人集成（可选）

### 10.1 创建飞书应用

1. 打开 https://open.feishu.cn/，登录飞书开放平台
2. 点"开发者后台" → "创建企业自建应用"
3. 名称填"认证查询机器人"，点"创建"
4. 记录 App ID 和 App Secret（在"凭证与基础信息"页面）

### 10.2 开通权限

左侧"权限管理" → 搜索并勾选：
- `im:message`
- `im:message.group_at_msg`
- `im:message.p2p_msg`
点"批量开通"

### 10.3 配置事件回调

1. 左侧"事件与回调" → 编辑"事件回调地址"
2. 填入：`https://你的域名/feishu/webhook`
   如 `https://seeed-cert-agent.onrender.com/feishu/webhook`
3. 记录 Verification Token
4. 添加事件 → 搜索 `im.message.receive_v1` → 勾选

### 10.4 配置环境变量

在 Render 的 "Environment" 页面添加：

| Key | Value |
|-----|-------|
| FEISHU_APP_ID | 你的 App ID |
| FEISHU_APP_SECRET | 你的 App Secret |
| FEISHU_VERIFICATION_TOKEN | 你的 Token |

### 10.5 发布

"版本管理与发布" → "创建版本" → 提交审核 → 通过后即可使用

### 10.6 使用

在飞书里找到机器人，发送 SKU 号如 `114993117`，机器人回复认证信息

---

## 最简启动方式：双击 启动服务.bat

项目文件夹里的 `启动服务.bat` 有交互菜单：

```
============================================
  Seeed Studio 认证查询服务
============================================

请选择操作：
  1. 启动 Web 查询服务
  2. 全量抓取所有 SKU 认证数据
  3. 提取认证 PDF 中的认证号
  4. 导出 Excel 认证映射表
  5. 退出

请输入数字 (1-5):
```

首次运行会自动安装依赖。输入数字即可执行对应操作。

---

## 命令速查表

打开命令行（Win+R → cmd）后，先进入项目文件夹：

```
cd /d C:\Users\seeed\Documents\Codex\2026-08-12\h\outputs\seeed-cert-agent
```

| 操作 | 命令 |
|------|------|
| 安装依赖 | `pip install -r requirements.txt` |
| 全量抓取 | `python scraper.py` |
| 强制重新抓取 | `python scraper.py --force` |
| 查单个 SKU | `python scraper.py --sku 114993117` |
| 提取认证号 | `python export.py --extract-pdfs` |
| 强制重新提取 | `python export.py --extract-pdfs --force` |
| 导出 Excel | `python export.py --excel` |
| 启动 Web 服务 | `python app.py --host 0.0.0.0 --port 5000` |
| 指定端口启动 | `python app.py --port 8080` |
| 浏览器访问 | http://localhost:5000 |
| 开放防火墙(管理员) | `netsh advfirewall firewall add rule name="Seeed Cert Agent" dir=in action=allow protocol=TCP localport=5000` |

---

## 可测试的 SKU

| SKU | 产品 | 认证 |
|-----|------|------|
| 114993117 | EdgeBox RPi 200 | CE, EUDOC, FCC, ROHS, UKDOC |
| 102110207 | Raspberry Pi 3 Model A+ | EUDOC, ROHS, UKDOC |
| 114992543 | 4-Channel SPDT Relay HAT | ROHS |

---

## 常见问题

### Q: 输入 python 提示"不是内部或外部命令"
重新运行 Python 安装程序，勾选 "Add Python to PATH"

### Q: pip install 报错
用 `python -m pip install -r requirements.txt`

### Q: 启动报错"Address already in use"
换端口：`python app.py --port 8080`，浏览器开 http://localhost:8080

### Q: 浏览器打开 localhost:5000 无法访问
1. 确认命令行窗口还开着
2. 确认显示"Running on http://127.0.0.1:5000"
3. Windows 弹防火墙提示 → 点"允许访问"

### Q: 查询某个 SKU 提示"未找到"
SKU 是 9 位纯数字（如 114993117），不是 URL 里的产品 ID（如 5729）

### Q: Excel 中认证号显示"未提取"
还没运行第5步。运行 `python export.py --extract-pdfs` 即可

### Q: Excel 中认证号显示"图片型PDF"
该 PDF 是扫描图片格式，无法自动提取文字，需手动打开 PDF 查看

### Q: 全量抓取太慢
正常 60-80 分钟（3100 个产品）。加快：`python scraper.py --delay 0.5`
中断后重运行会自动跳过已抓取的

### Q: 内网同事无法访问
1. 确认防火墙端口已开放（第8步）
2. 确认服务用 `--host 0.0.0.0` 启动
3. 确认电脑和同事在同一个局域网
4. 确认电脑没关机/休眠
